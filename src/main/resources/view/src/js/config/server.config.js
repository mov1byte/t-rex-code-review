'use strict';

const SERVER_PROTOCOL = 'http';
const SERVER_DOMAIN_NAME = 'localhost';
const SERVER_PORT = 8080;
const SERVER_ADDRESS = `${SERVER_PROTOCOL}://${SERVER_DOMAIN_NAME}:${SERVER_PORT}`;

export default SERVER_ADDRESS;